{
  "title": "AIBOOSTER Runner Input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "code": {
      "title": "Code",
      "type": "string",
      "description": "JavaScript (async, ES2022) generated by the orchestrator LLM. Executed in a sandbox with the helper SDK (http, parse, save, log, llm, browse).",
      "editor": "javascript"
    },
    "params": {
      "title": "Params",
      "type": "object",
      "description": "Arbitrary params passed from the orchestrator. Available inside the code as `params`.",
      "default": {},
      "editor": "json"
    }
  },
  "required": ["code"]
}
